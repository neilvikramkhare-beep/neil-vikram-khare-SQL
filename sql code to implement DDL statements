USE Greaze_database_company; 
CREATE TABLE EMPLOYEE (Emp_no INT PRIMARY KEY, E_name VARCHAR(200),E_address VARCHAR(300),E_ph_no INT(10),Dep_no INT,Dep_name VARCHAR(100), job_id CHAR(5), salary DECIMAL(10, 2));
ALTER TABLE EMPLOYEE
ADD HIREDATE DATE;
INSERT INTO EMPLOYEE (Emp_no, E_name, Dep_no, salary, HIREDATE) 
VALUES (101, 'John Doe', 10, 50000.00, '2023-01-15');
UPDATE EMPLOYEE SET salary = 55000.00 WHERE Emp_no = 101;
SELECT E_name, Dep_name, salary FROM EMPLOYEE;
SELECT * FROM EMPLOYEE WHERE Dep_no = 10;
SELECT E_name, salary FROM EMPLOYEE ORDER BY salary DESC;
COMMIT;
ROLLBACK; 
ALTER TABLE EMPLOYEE DROP COLUMN E_ph_no;
Rename the table
RENAME TABLE EMPLOYEE TO Employee_Details;
TRUNCATE TABLE EMPLOYEE;
DROP TABLE EMPLOYEE;
